<%= simple_form_for(@article) do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.button :submit, class: "btn btn-outline-secondary btn-large" %>
  <%= link_to "Back", articles_path, :class => "btn btn-outline-secondary btn-large" %>
<% end %>